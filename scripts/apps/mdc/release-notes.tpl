自动构建的 fnOS 安装包

- 基于 [mdcng/mdc v${VERSION}](https://github.com/Helenvin/mdc-ng/releases/tag/v${VERSION})
- 平台: fnOS（Docker 容器，镜像支持 amd64 / arm64）
- 默认端口: ${DEFAULT_PORT}
- 原地替换：安装向导默认接管既有手动部署的配置与媒体库路径${REVISION_NOTE}
${CHANGELOG}
