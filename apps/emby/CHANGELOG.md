## 2026-09-14

- 支持预置 Emby 插件：打包时内置 `apps/emby/plugins/` 下的插件，安装后首次启动同步到数据目录
- 改为 root 运行，可直接扫描 `/vol1/1000` 等共享目录，无需手动授权路径

## 2026-02-10

- ⚠️ 改为以 `EmbyServer` 用户运行（非 root），提升安全性
- 升级时自动修正数据目录权限，无需手动操作
