#!/bin/bash
# AVdb Docker app — wizard integration (drop-in 接管既有部署)

service_postinst() {
    # 数据目录默认指向手动部署既有路径，缺失时兜底创建
    if [ -n "${wizard_data_dir:-}" ]; then
        mkdir -p "${wizard_data_dir}" 2>/dev/null || true
        install_log "AVdb 数据目录: ${wizard_data_dir}"
    fi
}
